// In the startBot function, update this line:
setStatus(data.botStatus || data.status); // Handle both old and new property names

// In the stopBot function, update this line:
setStatus(data.botStatus || data.status);

// In the tickBot function, update this line:
setStatus(data.botStatus || data.status);
